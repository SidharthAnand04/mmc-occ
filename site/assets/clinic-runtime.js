document.addEventListener('DOMContentLoaded',()=>{
 document.querySelectorAll('.mmc-inquiry').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const fields=new FormData(form);const body=[...fields].map(([key,value])=>`${key}: ${value}`).join('\n');const result=form.querySelector('.inquiry-result');result.querySelector('pre').textContent=body;result.querySelector('.draft-link').href='mailto:info@mymedicalclinicmn.com?subject='+encodeURIComponent(form.dataset.kind||'Clinic inquiry')+'&body='+encodeURIComponent(body);result.hidden=false;result.scrollIntoView({behavior:'smooth',block:'center'});}));
 document.querySelectorAll('form[data-local-only]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();let message=form.querySelector('[role=status]');if(!message){message=document.createElement('p');message.setAttribute('role','status');form.append(message);}message.textContent='Nothing has been sent or saved. Please call 651-340-1445 for the current medical intake process.';}));
 document.querySelectorAll('.mainmenu li.menu-item-has-children>a:first-child').forEach(a=>{a.addEventListener('keydown',e=>{if(e.key==='ArrowDown'){e.preventDefault();a.parentElement.classList.add('menu-open');a.parentElement.querySelector('ul a')?.focus();}});});
 document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.menu-open').forEach(el=>el.classList.remove('menu-open'));});
 document.querySelectorAll('a.load_more').forEach(a=>a.addEventListener('click',async e=>{e.preventDefault();try{const response=await fetch(a.href);if(!response.ok)throw Error();const next=new DOMParser().parseFromString(await response.text(),'text/html');const target=document.querySelector('#load-data');next.querySelectorAll('#load-data>.container').forEach(el=>target.append(el));const nextLink=next.querySelector('a.load_more');if(nextLink)a.href=nextLink.href;else a.remove();}catch{location.href=a.href;}}));
});
